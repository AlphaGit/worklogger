```json
{
    "inputs": [...],
    "transformations": [{
        "condition": {
            "type": "matches",
            "regex": "something"
        },
        "action": {
            "type": "addTag",
            "tagToAdd": "something"
        }
    }],
}
```
